apiVersion: v1
kind: Service
metadata:
  name: api-gateway
spec:
  selector:
    app: kong-deployment
  ports:
    - name: kong
      port: 8000
      targetPort: 8000
    - name: konga
      port: 1337
      targetPort: 1337
  type: ClusterIP